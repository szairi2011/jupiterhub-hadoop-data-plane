<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!--
  Hive Metastore configuration — Phase 3
  This file is mounted read-only into the hive-metastore container.
  It is also mounted into spark-worker so Spark executors can locate the HMS.

  Service topology:
    postgres          :5432  — metastore backing DB
    hive-metastore    :9083  — HMS thrift server (clients connect here)
    spark-worker             — executors write to the shared warehouse volume
-->
<configuration>

  <!-- ======================================================================
       PostgreSQL backend
       ====================================================================== -->
  <property>
    <name>javax.jdo.option.ConnectionURL</name>
    <value>jdbc:postgresql://postgres:5432/metastore</value>
    <description>JDBC URL for the metastore backing database.</description>
  </property>

  <property>
    <name>javax.jdo.option.ConnectionDriverName</name>
    <value>org.postgresql.Driver</value>
  </property>

  <property>
    <name>javax.jdo.option.ConnectionUserName</name>
    <value>hive</value>
  </property>

  <property>
    <name>javax.jdo.option.ConnectionPassword</name>
    <value>hive</value>
  </property>

  <!-- ======================================================================
       Warehouse directory
       Phase 5: warehouse moves from a Docker volume to HDFS.
       Run scripts/phase5-init-hdfs.ps1 once to create this directory.
       Phase 3 baseline was: file:///opt/hive/data/warehouse (Docker volume).
       ====================================================================== -->
  <property>
    <name>hive.metastore.warehouse.dir</name>
    <value>hdfs://namenode:9000/user/hive/warehouse</value>
  </property>

  <!-- ======================================================================
       Schema management
       apache/hive:4.0.0 entrypoint runs schematool -initOrUpgradeSchema
       automatically on startup. Disable verification at query time (dev only).
       ====================================================================== -->
  <property>
    <name>hive.metastore.schema.verification</name>
    <value>false</value>
  </property>

  <!-- ======================================================================
       Performance (dev settings — not for production)
       ====================================================================== -->
  <!-- Disable column statistics auto-gather — speeds up CREATE TABLE and INSERT -->
  <property>
    <name>hive.stats.autogather</name>
    <value>false</value>
  </property>

  <!-- Allow clients that don't pass a Kerberos token (Phase 5 will restrict this) -->
  <property>
    <name>hive.metastore.sasl.enabled</name>
    <value>false</value>
  </property>

</configuration>
